// React is not needed since Vite handles it automatically, but let's keep the file valid
import Dashboard from './pages/Dashboard';

function App() {
  return (
    <div className="app-container">
      <Dashboard />
    </div>
  );
}

export default App;
